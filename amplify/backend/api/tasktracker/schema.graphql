#2) Create a task with a title, description, status (completed, in progress, etc.), and due date
type Task @model {
  id: ID!
  title: String!
  description: String!
  status: String!
  dueDate: AWSDateTime!
}